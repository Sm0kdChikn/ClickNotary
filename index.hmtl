<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <meta name="description" content="ClickNotary – Fast, secure Remote Online Notarization (RON). Notarize documents online in minutes."/>
  <title>ClickNotary – Remote Online Notary | Fast & Secure RON</title>

  <!-- Favicon – upload your logo as favicon.ico or logo.png later -->
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>✍</text></svg>">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">

  <style>
    :root {
      --blue: #0055ff;        /* Exact logo blue */
      --blue-dark: #0044cc;   /* Hover / darker states */
      --cyan: #00ccff;        /* "click" part of your logo */
      --dark: #1e293b;
      --gray: #64748b;
      --light: #f8fafc;
      --white: #ffffff;
    }
    * { margin:0; padding:0; box-sizing:border-box; }
    body {
      font-family: 'Inter', sans-serif;
      line-height: 1.6;
      color: var(--dark);
      background: var(--white);
    }
    .container { max-width: 1200px; margin: 0 auto; padding: 0 1.5rem; }

    /* Header */
    header {
      background: white;
      box-shadow: 0 4px 20px rgba(0,85,255,0.08);
      position: fixed;
      width: 100%;
      top: 0;
      z-index: 1000;
    }
    nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.2rem 0;
    }
    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 2.1rem;
      font-weight: 700;
      text-decoration: none;
    }
    .logo .click { color: var(--cyan); }
    .logo .notary { color: var(--blue); }
    .nav-links a {
      text-decoration: none;
      color: var(--dark);
      font-weight: 500;
      margin-left: 2rem;
      transition: color 0.3s;
    }
    .nav-links a:hover { color: var(--blue); }
    .cta-button {
      background: var(--blue);
      color: white;
      padding: 0.75rem 1.8rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s;
    }
    .cta-button:hover {
      background: var(--blue-dark);
      transform: translateY(-2px);
    }

    /* Hero */
    .hero {
      padding: 10rem 0 7rem;
      text-align: center;
      background: linear-gradient(135deg, #f8fbff 0%, #f0f7ff 100%);
    }
    .hero h1 {
      font-family: 'Playfair Display', serif;
      font-size: 3.8rem;
      margin-bottom: 1.2rem;
      color: var(--dark);
    }
    .hero p {
      font-size: 1.3rem;
      color: var(--gray);
      max-width: 720px;
      margin: 0 auto 2.5rem;
    }
    .btn-primary {
      background: var(--blue);
      color: white;
      padding: 1.1rem 2.4rem;
      border: none;
      border-radius: 0.5rem;
      font-size: 1.1rem;
      font-weight: 600;
      text-decoration: none;
      display: inline-block;
      transition: all 0.3s;
    }
    .btn-primary:hover {
      background: var(--blue-dark);
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(0,85,255,0.25);
    }
    .btn-secondary {
      background: transparent;
      color: var(--blue);
      border: 2px solid var(--blue);
    }
    .btn-secondary:hover {
      background: var(--blue);
      color: white;
    }

    /* Sections */
    .section-title {
      text-align: center;
      font-family: 'Playfair Display', serif;
      font-size: 2.8rem;
      margin-bottom: 3.5rem;
      color: var(--dark);
    }
    .features, .how-it-works, .compliance {
      padding: 7rem 0;
    }
    .features { background: white; }
    .how-it-works { background: #f8fbff; }
    .compliance { background: white; }

    .grid, .steps {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
    }
    .card, .step {
      background: white;
      padding: 2.5rem 2rem;
      border-radius: 1rem;
      box-shadow: 0 10px 30px rgba(0,85,255,0.08);
      text-align: center;
      transition: all 0.3s;
    }
    .card:hover, .step:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 40px rgba(0,85,255,0.15);
    }
    .icon { font-size: 3.8rem; margin-bottom: 1rem; }
    .step { position: relative; }
    .step::before {
      counter-increment: step;
      content: counter(step);
      position: absolute;
      top: -20px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--blue);
      color: white;
      width: 56px;
      height: 56px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.8rem;
      font-weight: bold;
    }

    /* Final CTA */
    .final-cta {
      background: linear-gradient(135deg, var(--blue), var(--blue-dark));
      color: white;
      text-align: center;
      padding: 8rem 0;
    }
    .final-cta h2 {
      font-family: 'Playfair Display', serif;
      font-size: 3.2rem;
      margin-bottom: 1rem;
    }

    /* Footer */
    footer {
      background: var(--dark);
      color: #cbd5e1;
      padding: 4rem 0 2rem;
      text-align: center;
    }
    .footer-links a {
      color: #cbd5e1;
      margin: 0 1rem;
      text-decoration: none;
    }
    .footer-links a:hover { color: var(--cyan); }

    @media (max-width: 768px) {
      .nav-links { display: none; }
      .hero h1 { font-size: 2.9rem; }
      .hero .buttons { flex-direction: column; align-items: center; }
      .btn-primary, .btn-secondary { width: 90%; max-width: 320px; }
    }
  </style>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"/>
</head>
<body>

  <header>
    <div class="container">
      <nav>
        <a href="#" class="logo"><span class="click">click</span><span class="notary">Notary</span></a>
        <div class="nav-links">
          <a href="#how">How It Works</a>
          <a href="#pricing">Pricing</a>
          <a href="#security">Security</a>
          <a href="mailto:hello@clicknotary.com">Contact</a>
        </div>
        <a href="#book" class="cta-button">Book Now</a>
      </nav>
    </div>
  </header>

  <section class="hero">
    <div class="container">
      <h1>Get Notarized Online in Minutes</h1>
      <p>Secure, legal Remote Online Notarization available 24/7.<br>No travel. No waiting. Just click and sign.</p>
      <div class="buttons" style="display:flex;gap:1.5rem;justify-content:center;flex-wrap:wrap;margin-top:2rem;">
        <a href="#book" class="btn-primary">Start Notarization Now</a>
        <a href="#how" class="btn-primary btn-secondary">How It Works</a>
      </div>
    </div>
  </section>

  <section class="features">
    <div class="container">
      <h2 class="section-title">Why Thousands Choose ClickNotary</h2>
      <div class="grid">
        <div class="card">
          <div class="icon">Fast</div>
          <h3>Instant Connection</h3>
          <p>Connect with a licensed notary in under 60 seconds – anytime.</p>
        </div>
        <div class="card">
          <div class="icon">Secure</div>
          <h3>Bank-Grade Security</h3>
          <p>256-bit encryption + KBA identity verification on every session.</p>
        </div>
        <div class="card">
          <div class="icon">Legal</div>
          <h3>100% Legal & Compliant</h3>
          <p>Accepted nationwide where RON is authorized.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="how-it-works" id="how">
    <div class="container">
      <h2 class="section-title">How It Works – 4 Simple Steps</h2>
      <div class="steps" style="counter-reset:step;">
        <div class="step"><h3>Upload Document</h3><p>Securely upload your unsigned PDF.</p></div>
        <div class="step"><h3>Verify Identity</h3><p>Quick knowledge-based authentication.</p></div>
        <div class="step"><h3>Meet Notary Live</h3><p>Video call with a licensed notary public.</p></div>
        <div class="step"><h3>Sign & Download</h3><p>Electronically sign and get your notarized file instantly.</p></div>
      </div>
    </div>
  </section>

  <section class="compliance" id="security">
    <div class="container">
      <h2 class="section-title">Security & Compliance First</h2>
      <p style="max-width:800px;margin:0 auto 3rem;font-size:1.1rem;color:var(--gray);">
        We use enterprise-grade encryption and follow strict state & federal RON laws.
      </p>
      <div style="display:flex;justify-content:center;gap:4rem;flex-wrap:wrap;">
        <div style="text-align:center;">
          <i class="fas fa-shield-alt" style="font-size:4rem;color:var(--blue);margin-bottom:1rem;"></i>
          <p><strong>SOC 2 Type II</strong><br>Certified</p>
        </div>
        <div style="text-align:center;">
          <i class="fas fa-lock" style="font-size:4rem;color:var(--blue);margin-bottom:1rem;"></i>
          <p><strong>256-bit AES</strong><br>Encryption</p>
        </div>
        <div style="text-align:center;">
          <i class="fas fa-gavel" style="font-size:4rem;color:var(--blue);margin-bottom:1rem;"></i>
          <p><strong>ESIGN & UETA</strong><br>Compliant</p>
        </div>
      </div>
    </div>
  </section>

  <section class="final-cta" id="book">
    <div class="container">
      <h2>Ready to Notarize?</h2>
      <p style="font-size:1.4rem;margin:1.5rem 0 2.5rem;">Start your secure online notarization session in under a minute.</p>
      <a href="mailto:hello@clicknotary.com?subject=I want to book a RON session&body=Hi, I'd like to schedule an online notarization." 
         class="btn-primary" style="font-size:1.3rem;padding:1.3rem 3.5rem;">
        Book Your Session Now
      </a>
    </div>
  </section>

  <footer>
    <div class="container">
      <p style="margin-bottom:1rem;font-size:1.1rem;">
        <strong><span style="color:var(--cyan);">click</span><span style="color:white;">Notary</span></strong> – Remote Online Notary Services
      </p>
      <div class="footer-links">
        <a href="#">Privacy Policy</a>
        <a href="#">Terms of Service</a>
        <a href="#">Support</a>
      </div>
      <p style="margin-top:2rem;font-size:0.9rem;">
        © 2025 ClickNotary. All rights reserved.
      </p>
    </div>
  </footer>

</body>
</html>